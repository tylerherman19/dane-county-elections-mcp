{
  "name": "dane-county-elections-mcp",
  "version": "1.0.0",
  "description": "Model Context Protocol (MCP) server for the Dane County Elections API",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "dane-county-elections-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "start": "node dist/index.js",
    "dev": "tsc && node dist/index.js",
    "prepare": "npm run build"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "dane-county",
    "elections",
    "api"
  ],
  "author": "Tyler Herman",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
